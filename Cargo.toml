[package]
name = "agentscope-rust"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "A community-driven Rust implementation inspired by AgentScope"
license = "Apache-2.0"
readme = "README.md"
keywords = ["agent", "ai", "llm", "agentscope"]
categories = ["asynchronous"]
publish = false

[features]
default = []
sqlite = ["dep:tokio-rusqlite"]

[lib]
name = "agentscope"
path = "src/lib.rs"

[dependencies]
async-stream = "0.3.6"
base64 = "0.22"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
futures-core = "0.3"
futures-util = "0.3"
httpdate = "1"
jsonschema = { version = "0.50", default-features = false }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["time"] }
tokio-rusqlite = { version = "0.7", features = ["bundled"], optional = true }
url = { version = "2", features = ["serde"] }
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
futures-executor = "0.3"
tokio = { version = "1", features = ["io-util", "macros", "net", "rt-multi-thread", "sync"] }
tempfile = "3"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
